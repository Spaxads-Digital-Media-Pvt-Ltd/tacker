/**
 * SINGLE SOURCE OF TRUTH for the product name in the frontend (spec §14). "Tracker" is a
 * working name and WILL be renamed — change it HERE and the whole UI follows.
 */
export const BRAND = {
  name: 'Tracker',
  tagline: 'Affiliate tracking, done right.',
} as const;
